JSVC_EXTRA_OPTS="-user unifi -nodetach -errfile /var/log/unifi_jsvc.err -outfile /var/log/unifi_jsvc.log"
for d in log run; do
  PATH=/var/$d/unifi
  [ -d $PATH ] || mkdir $PATH
  chown unifi:unifi $PATH
  chmod 755 $PATH
done
